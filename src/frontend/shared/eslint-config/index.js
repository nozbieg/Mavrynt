// @ts-check
/**
 * Default Mavrynt ESLint config — re-exports the React preset.
 * Apps and packages should import from `@mavrynt/eslint-config/react` or
 * `@mavrynt/eslint-config/library` to be explicit about their environment.
 */
import reactConfig from "./react.js";

export default reactConfig;
