/**
 * @mavrynt/design-tokens
 *
 * Public entry point. Re-exports the typed token values so apps can read them
 * programmatically (e.g. for chart palettes, JS-driven animations, or unit
 * tests). For styling, prefer the CSS imports under `./styles/*`.
 */
export * from "./tokens.ts";
