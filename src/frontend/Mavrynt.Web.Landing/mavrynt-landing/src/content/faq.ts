/**
 * FAQ item ids — copy lives under `faq.items.<id>.q` / `.a`.
 */
export type FaqId = "1" | "2" | "3" | "4" | "5" | "6";

export const faqIds: ReadonlyArray<FaqId> = ["1", "2", "3", "4", "5", "6"];
